# define a function which sorts a list of flags
# there are 3 colors of flags, all mixed up in the list
# the function will sort the flags such that
# all 'red' flags are left-aligned, 'blue' flags right-aligned
# 'green' flags are tugged in the middle

#i.e flags_pre_sort = ['green', 'red', 'blue', 'green']
# ==> flags_post_sort = ['red', 'green', 'green', 'blue']
